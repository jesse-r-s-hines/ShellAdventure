docker
six # TODO Required for "docker" module. We can remove this after the next release. See https://github.com/docker/docker-py/issues/2842
pyaml
ttkthemes
Pillow
dill
deepmerge
yamale>=3.0.7
python-lorem # This is only used inside the docker container, but require it so that is shows up for people developing puzzle templates
